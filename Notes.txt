errata
Dated matplotlib tutorial, updates to matplotlib enforce some code parameters which resulted in errors to animation examples
Dated seaborn tutorial, API changed, notebook version kernal incompatibility


github setup ssh:
https://help.github.com/articles/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent/

git config core.editor notepad

git add <file file>
git commit
git commit -m "one liner without adding text editor notes"
git commit -a #commit all files without adding
git push #push to github (not needed every time)
git status #list file changes
git diff #see changes
git diff <file> #changes to a particular file

#start a repository from scratch, navigate to directory then:
git init
git add
git commit

#Push. Log into github, click >new repository >add repository
#Follow instruction: ...or push an exisiting repository from the command line
git remote add origin https://github.com/csnyd/2017.git
git push -u origin
git push origin master #after commiting to git

Jupyter: https://www.cheatography.com/weidadeyue/cheat-sheets/jupyter-notebook/
something? help
something?? 

Matplotlib, additional practice: http://jakevdp.github.io/mpl_tutorial/tutorial_pages/tut3.html
colormap 'viridis'